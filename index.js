//Text To Number

myButton = document.getElementById("year"); //searches for and detects the input element from the 'year' id
myButton.type = "number"; // value will be change as Number


//Number To text

myButton = document.getElementById("year"); //searches for and detects the input element from the 'year' id
myButton.type = "text"; //Number will be change as text